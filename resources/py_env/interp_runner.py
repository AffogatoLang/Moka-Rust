#!/bin/python

import sys

argnum = len(sys.argv);

if argnum != 3:
    raise Exception("Invalid Number Of Arguments Provided");

# Init code for userlang interp funcs

# Create decorators

# Set syspath and load module passed as argv[1]

# Inject decorators into module

# Call module stuff in sequence
